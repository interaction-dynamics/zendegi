import Head from 'next/head'

export const metadata = {
  title: 'Confirmed - Zendegi',
  // description: 'Generated by create next app',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
