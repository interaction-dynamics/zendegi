{
  "name": "zendegi",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean && rm -rf node_modules",
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "8.43.0",
    "eslint-config-next": "13.4.7",
    "prettier": "^2.7.1",
    "turbo": "^1.9.3"
  }
}
